pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }

    plugins {
        id "io.quarkus" version "${quarkusVersion}"
    }
}

rootProject.name='quarkus-websockets-bug'
